---
import Centered from '@/layouts/Centered.astro';

import type { Metadata } from '@/types/common';

export interface Props extends Metadata {}

const props = Astro.props;
---

<Centered metadata={props}>
  <article>
    <slot />
  </article>
</Centered>
