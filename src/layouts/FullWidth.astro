---
import Base from '@/layouts/Base.astro';

import type { BaseProps } from '@/layouts/Base.astro';

export interface Props extends BaseProps {}
const props = Astro.props;
---

<Base {...props}>
  <main id="main" class="flex-grow flex justify-center w-full">
    <slot />
  </main>
</Base>
